import { FormLabel, FormHelperText, RadioGroup, HStack, NumberInputField, NumberInputStepper, NumberIncrementStepper, NumberDecrementStepper } from "../src/index";
import { FormControl } from "~/form-control";
import { Input } from "~/input";
import { Radio } from "~/radio";
import { Select } from "~/select";
import { NumberInput } from "~/number-input";

import React from 'react';
import { mdx } from '@mdx-js/react';
import { Playground } from '@divriots/dockit-react/playground';
import Layout from '~/doc-layout';
export default Layout;

#  Form Control

FormControl provides context such as `isInvalid`, `isDisabled`, and `isRequired`
to form elements.

It follows the [WAI specifications](https://www.w3.org/WAI/tutorials/forms/) for
forms.





## Import

```js
import {
  FormControl,
  FormLabel,
  FormErrorMessage,
  FormHelperText,
} from "@chakra-ui/react"
```

## Usage

<Playground
scope={{ FormControl, FormLabel, Input, FormHelperText }}
code={`
<FormControl id="email">
  <FormLabel>Email address</FormLabel>
  <Input type="email" />
  <FormHelperText>We'll never share your email.</FormHelperText>
</FormControl>
`}
/>

### Sample usage for a radio or checkbox group

<Playground
scope={{ FormControl, FormLabel, RadioGroup, HStack, Radio, FormHelperText }}
code={`
<FormControl as="fieldset">
  <FormLabel as="legend">Favorite Naruto Character</FormLabel>
  <RadioGroup defaultValue="Itachi">
    <HStack spacing="24px">
      <Radio value="Sasuke">Sasuke</Radio>
      <Radio value="Nagato">Nagato</Radio>
      <Radio value="Itachi">Itachi</Radio>
      <Radio value="Sage of the six Paths">Sage of the six Paths</Radio>
    </HStack>
  </RadioGroup>
  <FormHelperText>Select only if you're a fan.</FormHelperText>
</FormControl>
`}
/>

### Making a field required

By passing the `isRequired` props, the `Input` field has `aria-required` set to
`true`, and the `FormLabel` will show a red asterisk.

<Playground
scope={{ FormControl, FormLabel, Input }}
code={`
<FormControl id="first-name" isRequired>
  <FormLabel>First name</FormLabel>
  <Input placeholder="First name" />
</FormControl>
`}
/>

### Select Example

<Playground
scope={{ FormControl, FormLabel, Select }}
code={`
<FormControl id="country">
  <FormLabel>Country</FormLabel>
  <Select placeholder="Select country">
    <option>United Arab Emirates</option>
    <option>Nigeria</option>
  </Select>
</FormControl>
`}
/>

### Number Input Example

<Playground
scope={{ FormControl, FormLabel, NumberInput, NumberInputField, NumberInputStepper, NumberIncrementStepper, NumberDecrementStepper }}
code={`
<FormControl id="amount">
  <FormLabel>Amount</FormLabel>
  <NumberInput max={50} min={10}>
    <NumberInputField />
    <NumberInputStepper>
      <NumberIncrementStepper />
      <NumberDecrementStepper />
    </NumberInputStepper>
  </NumberInput>
</FormControl>
`}
/>